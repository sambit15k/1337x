export REPO="owner/repo"
export EVENT="workflow_run"
export ACTOR="your-gh-username"
export REF="refs/heads/main"
export RUN_URL="https://github.com/owner/repo/actions/runs/123456789"
export GITHUB_EVENT_PATH="$(pwd)/sample_workflow_run_success.json"

python .github/scripts/slack_payload.py | tee /tmp/slack_payload.json

# Optional: post to Slack (be careful – this sends a live message)
curl -s -X POST -H 'Content-type: application/json' \
  --data @/tmp/slack_payload.json "$SLACK_WEBHOOK_URL"